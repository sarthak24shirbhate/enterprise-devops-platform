apiVersion: v1
kind: Service
metadata:
  name: enterprise-python-service
spec:
  type: LoadBalancer
  selector:
    app: enterprise-python-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
